FROM FROM prefecthq/prefect:3.4.8-python3.12

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    libgl1 \
    libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /yolo_auto_label

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt